<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.workyoutube">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WorkYoutube">
        <activity android:name=".DatePickerActivity"></activity>
        <activity android:name=".TextActivity" />
        <activity android:name=".textFragmnt" />
        <activity android:name=".TitleFragmentPrint" />
        <activity android:name=".TextFragment" />
        <activity android:name=".TitleActivity" />
        <activity android:name=".CoastOfArmsFragment" />
        <activity android:name=".CitiesFragment" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>